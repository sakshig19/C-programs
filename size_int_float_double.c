#include <stdio.h>
int main(){
    int a;
    float f;
    double d;
    printf("The size of integer is %d \n",sizeof(a));
    printf("The size of float is %d \n",sizeof(f));
    printf("The size of double is %d \n",sizeof(d));
    return 0;
}